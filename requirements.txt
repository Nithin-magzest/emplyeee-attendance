# ── Web framework ─────────────────────────────────────────────────────────────
# Floors below are the lowest version WITHOUT known CVEs as of the last
# `pip-audit` pass (see SECURITY_NOTES section in the dependency report) —
# bump them further if pip-audit finds new advisories, don't lower them.
flask>=3.1.3
flask-cors>=6.0.5
flask-limiter>=3.5.0
limits>=5.8.0
werkzeug>=3.1.8
gunicorn>=23.0.0

# ── Database ───────────────────────────────────────────────────────────────────
psycopg2-binary>=2.9.9

# ── Face recognition & image processing ───────────────────────────────────────
opencv-python>=4.8.1.78
face-recognition>=1.3.0
numpy>=1.22.0
pillow>=12.2.0

# ── QR codes ──────────────────────────────────────────────────────────────────
qrcode>=8.2

# ── Spreadsheets & reports ────────────────────────────────────────────────────
openpyxl>=3.1.5
# Dependabot-proposed major bump (3.x -> 5.x) — floor raised per its advisory,
# but NOT locally verified: this dev sandbox's Python 3.7 can't even install
# reportlab 5.x (needs >=3.9), so PDF generation (payslips, offer letters,
# onboarding docs — see utils/salary_utils.py, blueprints/onboarding.py) needs
# a real smoke test against 5.x before this is trusted, via CI or a
# Python 3.11 environment matching the production container.
reportlab>=5.0.0,<6.0.0

# ── Cryptography & auth ───────────────────────────────────────────────────────
cryptography>=46.0.7
webauthn>=2.1.0
bcrypt>=5.0.0
pyotp>=2.9.0

# ── Cache / rate-limit backend ────────────────────────────────────────────────
redis>=5.0.0

# ── Malware scanning ──────────────────────────────────────────────────────────
clamd>=1.0.2

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.2.2
python-json-logger>=4.1.0
setuptools>=83.0.0
typing-extensions>=4.7.0

# ── AWS Secrets Manager (production secret loading — see utils/secrets_loader.py)
boto3>=1.34.0

# ── AI assistant (employee chat widget — see utils/ai_assistant.py) ────────────
# No package needed: talks to the Anthropic API directly over HTTPS via
# urllib.request (stdlib), the same pattern already used for webhook
# delivery in utils/alerts.py.
